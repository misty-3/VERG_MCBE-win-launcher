<Window x:Class="MCLauncher.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MCLauncher"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        MinWidth="400"
        WindowStartupLocation="CenterOwner"
        Title="Cleanup"
        Background="#161925">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Cleanup" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,16"/>

        <TextBlock Text="Cleaning up system for installing Minecraft from Microsoft Store..." 
                   Foreground="#8b95b8" 
                   TextWrapping="Wrap"
                   Margin="0,0,0,16" 
                   Grid.Row="1" />
                   
        <ProgressBar Name="progressBar" 
                     Grid.Row="2" 
                     Height="6" 
                     IsIndeterminate="True"
                     Foreground="#3b82f6"
                     Background="#12141c"
                     BorderThickness="0"/>
    </Grid>
</Window>
